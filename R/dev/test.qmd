---
title: "Untitled"
format: html
---

```{r}
data <- read.csv("data/data.csv")

sub1 <- data |> dplyr::filter(subj_id == 1)

test <- metaSDT::matrix_freq(
  stim = sub1$stim,
  resp = sub1$resp,
  conf = sub1$conf 
)
```
