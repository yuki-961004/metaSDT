% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shell_run_m.R
\name{shell_run_m}
\alias{shell_run_m}
\title{Simulate trial-level SDT data without fitting}
\usage{
shell_run_m(params, model = "sdt", option = list())
}
\arguments{
\item{params}{A structured parameter list with \code{free}, \code{fixed}, and
\code{constant} entries, or a flat named list interpreted as free parameters.}

\item{model}{Model name. Currently, \code{"sdt"} is the main supported path.}

\item{option}{Shell-level options. See \code{\link[=default_shell_run_m_option]{default_shell_run_m_option()}}.}
}
\value{
An S3 object with class \code{"metaSDT.shell_run_m"}.
}
\description{
\code{shell_run_m()} is a high-level simulation shell for metaSDT workflows. It
simulates trial-level data from model parameters and does not estimate
parameters. It is intended as Step 1 of the future metaSDT modeling workflow:
generate and inspect model-implied data before fitting estimators.
}
\details{
The \code{option} argument controls shell-level simulation and display behavior,
such as the number of simulated trials and whether to draw the density plot.
It is separate from estimator \code{control}, which is reserved for algorithm
settings in functions such as \code{\link[=estimate_mle]{estimate_mle()}} and
\code{\link[=estimate_map]{estimate_map()}}.

Currently, \code{"sdt"} is the main supported model path.
}
\examples{
fit <- shell_run_m(
  params = list(
    free = list(d = 1.5, c_resp = 0.0, c_conf = c(0.5, 1.0, 1.5)),
    fixed = list(sd_signal = 1.0, sd_noise = 1.0)
  ),
  model = "sdt",
  option = list(n = 1000, seed = 123, plot = FALSE)
)
}
